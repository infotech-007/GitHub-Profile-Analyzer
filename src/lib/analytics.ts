import type { GitHubRepo } from '@/types/github';
export function repoAnalytics(repos:GitHubRepo[]){const totalStars=sum(repos,'stargazers_count'),totalForks=sum(repos,'forks_count'),totalWatchers=sum(repos,'watchers_count'),totalIssues=sum(repos,'open_issues_count');return{total:repos.length,totalStars,totalForks,totalWatchers,totalIssues,mostStarred:max(repos,'stargazers_count'),mostForked:max(repos,'forks_count')}}
function sum(r:GitHubRepo[],k:keyof GitHubRepo){return r.reduce((a,x)=>a+Number(x[k]||0),0)}function max(r:GitHubRepo[],k:keyof GitHubRepo){return[...r].sort((a,b)=>Number(b[k]||0)-Number(a[k]||0))[0]}
export function languageAnalytics(repos:GitHubRepo[]){const counts=new Map<string,number>();repos.forEach(r=>r.language&&counts.set(r.language,(counts.get(r.language)||0)+Math.max(1,r.size)));const total=[...counts.values()].reduce((a,b)=>a+b,0)||1;return[...counts].map(([name,value])=>({name,value,percent:Math.round(value/total*1000)/10})).sort((a,b)=>b.value-a.value)}
export function recommendations(repos:GitHubRepo[]){return{popular:[...repos].sort((a,b)=>b.stargazers_count-a.stargazers_count).slice(0,3),active:[...repos].sort((a,b)=>+new Date(b.updated_at)-+new Date(a.updated_at)).slice(0,3),best:[...repos].sort((a,b)=>(b.stargazers_count*3+b.forks_count*2)-(a.stargazers_count*3+a.forks_count*2)).slice(0,3)}}
