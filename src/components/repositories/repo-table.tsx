'use client';import{useMemo,useState}from'react';import{Card}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{dateFmt}from'@/lib/utils';import type{GitHubRepo}from'@/types/github';
export function RepoTable({repos}:{repos:GitHubRepo[]}){const[q,setQ]=useState(''),[lang,setLang]=useState('all'),[page,setPage]=useState(1),[sort,setSort]=useState<keyof GitHubRepo>('stargazers_count');const langs=['all',...Array.from(new Set(repos.map(r=>r.language).filter(Boolean))) as string[]];const filtered=useMemo(()=>repos.filter(r=>(lang==='all'||r.language===lang)&&(`${r.name} ${r.description}`.toLowerCase().includes(q.toLowerCase()))).sort((a,b)=>Number(b[sort]||0)-Number(a[sort]||0)),[repos,q,lang,sort]);const rows=filtered.slice((page-1)*10,page*10);return <Card><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><h2 className="text-xl font-bold">Repository Insights</h2><div className="flex gap-2"><Input placeholder="Search repositories" value={q} onChange={e=>setQ(e.target.value)}/><select className="rounded-xl border border-border bg-background px-3" value={lang} onChange={e=>setLang(e.target.value)}>{langs.map(l=><option key={l}>{l}</option>)}</select></div></div><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b border-border">{['name','language','stargazers_count','forks_count','open_issues_count','watchers_count','size','updated_at'].map(h=><th key={h} className="p-3"><button onClick={()=>setSort(h as keyof GitHubRepo)}>{h}</button></th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-b border-border"><td className="p-3"><a className="font-semibold text-indigo-500" href={r.html_url}>{r.name}</a><p className="text-xs text-slate-500">{r.archived?'Archived':r.private?'Private':'Public'}</p></td><td className="p-3">{r.language||'—'}</td><td className="p-3">{r.stargazers_count}</td><td className="p-3">{r.forks_count}</td><td className="p-3">{r.open_issues_count}</td><td className="p-3">{r.watchers_count}</td><td className="p-3">{r.size} KB</td><td className="p-3">{dateFmt(r.updated_at)}</td></tr>)}</tbody></table></div><div className="mt-4 flex justify-between text-sm"><button disabled={page===1} onClick={()=>setPage(p=>p-1)}>Previous</button><span>Page {page} of {Math.max(1,Math.ceil(filtered.length/10))}</span><button disabled={page>=Math.ceil(filtered.length/10)} onClick={()=>setPage(p=>p+1)}>Next</button></div></Card>}
