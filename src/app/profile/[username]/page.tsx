import{notFound}from'next/navigation';import{getEvents,getRepos,getUser}from'@/lib/github-api';import{getContributions}from'@/lib/github-graphql';import{ProfileDashboard}from'@/components/profile/profile-dashboard';
export default async function ProfilePage({params}:{params:Promise<{username:string}>}){const{username}=await params;try{const[user,repos,contributions,events]=await Promise.all([getUser(username),getRepos(username),getContributions(username),getEvents(username)]);return <ProfileDashboard user={user} repos={repos} contributions={contributions} events={events}/> }catch{return notFound()}}
